import { jsx as n } from "react/jsx-runtime";
import "react";
import t from "prop-types";
import l from "styled-components";
const a = l.main`
  // To prevent global outline on focus visible to force an outline when Main is focused
  &:focus-visible {
    outline: none;
  }
`, e = ({ labelledBy: o, ...i }) => /* @__PURE__ */ n(a, { "aria-labelledby": o || "main-content-title", id: "main-content", tabIndex: -1, ...i });
e.defaultProps = {
  labelledBy: void 0
};
e.propTypes = {
  labelledBy: t.string
};
export {
  e as Main
};
