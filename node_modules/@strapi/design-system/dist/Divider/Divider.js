import { jsx as n } from "react/jsx-runtime";
import "react";
import o from "prop-types";
import i from "styled-components";
import { Box as p } from "../Box/Box.js";
const a = i(p)`
  height: 1px;
  border: none;
  /* If contained in a Flex parent we want to prevent the Divider to shink */
  flex-shrink: 0;
  ${({ unsetMargin: r }) => r ? "margin: 0;" : ""}
`, e = ({ unsetMargin: r, ...t }) => /* @__PURE__ */ n(a, { ...t, as: "hr", unsetMargin: r });
e.defaultProps = {
  background: "neutral150",
  unsetMargin: !0
};
e.propTypes = {
  background: o.string,
  unsetMargin: o.bool
};
export {
  e as Divider
};
